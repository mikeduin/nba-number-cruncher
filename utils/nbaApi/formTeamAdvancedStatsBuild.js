export const formTeamAdvancedStatsBuild = (headers, team) => ({
  gp: team[headers.indexOf('GP')],
  w: team[headers.indexOf('W')],
  l: team[headers.indexOf('L')],
  w_pct: team[headers.indexOf('W_PCT')],
  min: team[headers.indexOf('MIN')],
  e_off_rating: team[headers.indexOf('E_OFF_RATING')],
  off_rating: team[headers.indexOf('OFF_RATING')],
  e_def_rating: team[headers.indexOf('E_DEF_RATING')],
  def_rating: team[headers.indexOf('DEF_RATING')],
  e_net_rating: team[headers.indexOf('E_NET_RATING')],
  net_rating: team[headers.indexOf('NET_RATING')],
  ast_pct: team[headers.indexOf('AST_PCT')],
  ast_to: team[headers.indexOf('AST_TO')],
  ast_ratio: team[headers.indexOf('AST_RATIO')],
  oreb_pct: team[headers.indexOf('OREB_PCT')],
  dreb_pct: team[headers.indexOf('DREB_PCT')],
  reb_pct: team[headers.indexOf('REB_PCT')],
  tm_tov_pct: team[headers.indexOf('TM_TOV_PCT')],
  efg_pct: team[headers.indexOf('EFG_PCT')],
  ts_pct: team[headers.indexOf('TS_PCT')],
  e_pace: team[headers.indexOf('E_PACE')],
  pace: team[headers.indexOf('PACE')],
  pace_per40: team[headers.indexOf('PACE_PER40')],
  poss: team[headers.indexOf('POSS')],
  pie: team[headers.indexOf('PIE')],
  gp_rank: team[headers.indexOf('GP_RANK')],
  w_rank: team[headers.indexOf('W_RANK')],
  l_rank: team[headers.indexOf('L_RANK')],
  w_pct_rank: team[headers.indexOf('W_PCT_RANK')],
  min_rank: team[headers.indexOf('MIN_RANK')],
  off_rating_rank: team[headers.indexOf('OFF_RATING_RANK')],
  def_rating_rank: team[headers.indexOf('DEF_RATING_RANK')],
  net_rating_rank: team[headers.indexOf('NET_RATING_RANK')],
  ast_pct_rank: team[headers.indexOf('AST_PCT_RANK')],
  ast_to_rank: team[headers.indexOf('AST_TO_RANK')],
  ast_ratio_rank: team[headers.indexOf('AST_RATIO_RANK')],
  oreb_pct_rank: team[headers.indexOf('OREB_PCT_RANK')],
  dreb_pct_rank: team[headers.indexOf('DREB_PCT_RANK')],
  reb_pct_rank: team[headers.indexOf('REB_PCT_RANK')],
  tm_tov_pct_rank: team[headers.indexOf('TM_TOV_PCT_RANK')],
  efg_pct_rank: team[headers.indexOf('EFG_PCT_RANK')],
  ts_pct_rank: team[headers.indexOf('TS_PCT_RANK')],
  pace_rank: team[headers.indexOf('PACE_RANK')],
  pie_rank: team[headers.indexOf('PIE_RANK')],
  updated_at: new Date()
})